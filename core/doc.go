// Package core holds the provider-neutral types, interfaces and errors shared
// by every llmkit provider. Applications normally import the root llmkit
// package, which re-exports everything here under the same names.
package core
